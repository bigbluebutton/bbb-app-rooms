<!DOCTYPE html>
<html lang="en">

    <head>
    <title>BbbAppRooms</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css" integrity="sha384-DNOHZ68U8hZfKXOrtjWvjxusGo9WQnrNx2sqG0tfsghAvtVlRW3tvkXWZh58N9jp" crossorigin="anonymous">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

<body>
    <%= render "admins/components/header" %>
      <% if current_user == nil %>
    <div class="flex flex-col justify-between h-full font-sans pt-15">
        <div class="row flex justify-center content-start bn-background pb-40 pt-15">
            <div class="w-1/3">
                <div class="text-5xl pt-16 pb-16 font-light"> <%= t('default.admin.title') %> </div>
                <div class="border border-opacity-50 p-12 bg-white mb-4 rounded-lg">
                    <div class="border-b-2 border-grey-200 border-opacity-50 mb-4 pb-2">
                        <p class="font-sans text-2xl font-semibold uppercase"> <%= t('default.admin.signin') %> </p>
                    </div>
                    <%= form_tag adminsessions_path do |form| %>
                        <div>
                            <div class="border border-rounded mb-4">
                                <%= text_field_tag :username, "", class: "w-full h-12 pl-2", placeholder: "Username" %>
                            </div>
                            <div class = "border border-rounded">
                                <%= password_field_tag :password, nil, class: "w-full h-12 pl-2", placeholder: "Password" %>
                            </div>
                            <div class="">
                                <%= submit_tag "Login", class: "border border-green-500 bg-green-500 text-white rounded-md px-4 py-2 m-4 ml-0 mb-0 transition duration-500 ease select-none hover:bg-green-600 focus:outline-none focus:shadow-outline" %>
                            </div>
                        </div>
                    <% end %>
                </div>
                <% if !notice.nil? %>
                    <p class="rounded font-md mt-4 p-4 text-red-900 bg-red-200"><%= notice %></p>
                <% end %>
            </div>
        </div>
        <% else %>
            <%= render "admins/users" %>
        <% end %>
        <div class=""> <%= render "admins/components/footer" %> </div>
    </div>
</body>
</html>