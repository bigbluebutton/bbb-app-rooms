<%= form_with model: [@room, scheduled_meeting], class: "form fill" do |form| %>
  <% if scheduled_meeting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scheduled_meeting.errors.count, "error") %> prohibited this meeting from being saved:</h2>

      <ul>
        <% scheduled_meeting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group input-group col-6">
    <%= form.label :name %>&nbsp;<span class="text-muted small">*Required</span><br>
    <%= form.text_field :name, :required => true, size: "45", class: "form-control input col-6" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
